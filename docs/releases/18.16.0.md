# Sophyane 18.16.0

- Introduces robust provider discovery through agents, bound methods, wrappers, partials, and closures.
- Caches the real fallback provider on the interactive TUI as the session dispatcher.
- Uses the dispatcher's active provider state for every heartbeat, timeout, and response-provenance message.
- Keeps Gemini repair ownership visible while cloud rescue is actually generating.
- Preserves the 180-second local-model timeout and deep-agent sandbox runtime.
