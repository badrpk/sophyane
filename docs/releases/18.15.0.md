# Sophyane 18.15.0

- Makes active provider ownership explicit during validator repair.
- Keeps Gemini as the current repair provider until the repair sequence ends.
- Reports provider changes in real time instead of always showing the configured primary.
- Preserves the 180-second local-model timeout while using the actual active provider name in timeout errors.
- Adds response provenance messages showing which provider returned each result.
